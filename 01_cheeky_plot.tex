\documentclass{article}
\usepackage{tikz}
\usepackage[graphics, active, tightpage]{preview}
\PreviewEnvironment{tikzpicture}

\usepackage{tikz}
\usetikzlibrary{decorations.text}

\begin{document}
\begin{tikzpicture}[>=stealth]

  \fill[white] (-0.5,-0.5) rectangle ++(10,6.25);

  \draw[->] (-0.5,0) -- ++(10,0) node[midway, below] {Document Complexity};
  \draw[->] (0,-0.5) -- ++(0,6.25) node[midway, above, rotate=90] {Time to Create};
  \clip (0,0) rectangle ++(10,6.25);

  \draw[->,domain=0:9.5, smooth] plot (\x,{0.04*\x^2+1});
  \draw[->,domain=0:4.826, smooth] plot (\x,{1/(5-\x)});
  \draw[densely dashed, gray] (4.9,0) -- ++(0,5.75);

  \path[->,
  domain=0:7.75,
  postaction={decorate},
  decoration={
  text effects along path,
  text={\ \LaTeX\ },
  text align=
    right,
    text effects/.cd,
    text along path,
    every character/.style={fill=white, yshift=-0.5ex}}]
  plot (\x,{0.04*\x^2+1});

  \path[->,
  domain=0:3.5,
  postaction={decorate},
  decoration={
  text effects along path,
  text={\ MS Word\ },
  text align=
    right,
    text effects/.cd,
    text along path,
    every character/.style={fill=white, yshift=-0.5ex}}]
  plot (\x,{1/(5-\x)});

\end{tikzpicture}

\end{document}
